<view class="header">
  <button type="{{name === '1'? 'primary': 'default'}}" size="mini" bindtap="handleClick" data-name="1">{{left}}</button>
  <button type="{{name === '2'? 'primary': 'default'}}" size="mini" bindtap="handleClick" data-name="2">{{right}}</button>
</view>
